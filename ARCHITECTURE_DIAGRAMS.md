# FISHING System - Architecture & Flow Diagrams

## 🔐 Authentication Flow

```
┌─────────────────┐
│   User visits   │
│  /auth/login    │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│   Show Login Form       │
│ - Email input           │
│ - Password input        │
│ - CSRF token            │
└────────┬────────────────┘
         │
    User enters
    credentials
         │
         ▼
┌──────────────────────────┐
│ Validate Input           │
│ - Email format check     │
│ - Password length check  │
└────────┬─────────────────┘
         │
    ├─ Invalid? ──► Show Error ──────┐
    │                                  │
    └─ Valid? ──► Query Database      │
                        │
                        ▼
              ┌──────────────────────┐
              │ Find user by email   │
              │ Hash: password_verify│
              └──────────┬───────────┘
                         │
              ┌──────────┴──────────┐
              │                      │
         No Match          Password Match
              │                      │
              ▼                      ▼
      ┌─────────────┐        ┌──────────────────┐
      │ Show Error  │        │ Create Session   │
      │ "Not Found" │        │ - user_id        │
      └─────────────┘        │ - username       │
                             │ - email          │
                             │ - role           │
                             └────────┬─────────┘
                                      │
                                      ▼
                             ┌──────────────────┐
                             │ Redirect to:     │
                             │ /dashboard       │
                             └────────┬─────────┘
                                      │
                                      ▼
                             ┌──────────────────┐
                             │ Dashboard Ready  │
                             │ (User logged in) │
                             └──────────────────┘
```

## 🛡️ Protected Routes Flow

```
User Requests Protected Route
    │
    ▼
┌──────────────────────────┐
│  AuthFilter::before()    │
│  Check session for       │
│  user_id                 │
└────────┬─────────────────┘
         │
    ┌────┴────┐
    │          │
  No ID    Has ID
    │          │
    ▼          ▼
Redirect   ┌──────────────────┐
to login   │ Check Role       │
           │ (if restricted)  │
           └────────┬─────────┘
                    │
                ┌───┴────┐
                │         │
            Allowed   Not Allowed
                │         │
                ▼         ▼
            Continue  Redirect
            Request   /dashboard
```

## 📊 Database Relationships

```
USERS Table
├─ id (PK)
├─ username
├─ email
├─ password (bcrypt)
├─ role (admin/staff)
└─ is_active
    │
    └─────► SALES (1:N)
            ├─ id (PK)
            ├─ invoice_no
            ├─ user_id (FK)
            ├─ customer_name
            └─ total_amount
                │
                └─────► SALE_ITEMS (1:N)
                        ├─ id (PK)
                        ├─ sale_id (FK)
                        ├─ product_id (FK)
                        ├─ quantity
                        └─ unit_price
                            │
                            └─────► PRODUCTS (N:1)
                                    ├─ id (PK)
                                    ├─ sku
                                    ├─ name
                                    ├─ supplier_id (FK)
                                    ├─ cost_price
                                    ├─ selling_price
                                    └─ current_stock
                                        │
                                        └─────► SUPPLIERS (N:1)
                                                ├─ id (PK)
                                                ├─ name
                                                ├─ email
                                                └─ contact_person
                                                    │
                                                    └─────► PURCHASE_ORDERS (1:N)
                                                            ├─ id (PK)
                                                            ├─ po_number
                                                            ├─ supplier_id (FK)
                                                            └─ status
                                                                │
                                                                └─────► PO_ITEMS (1:N)
                                                                        ├─ id (PK)
                                                                        ├─ po_id (FK)
                                                                        ├─ product_id (FK)
                                                                        └─ quantity
```

## 🔄 POS Checkout Flow

```
┌───────────────────────┐
│  Customer Approach    │
│  POS Counter          │
└──────────┬────────────┘
           │
           ▼
┌──────────────────────────┐
│ Load Product Search      │
│ API: GET /api/products   │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Customer Selects Items   │
│ - Search product         │
│ - Add to cart            │
│ - Check stock in real    │
│   time                   │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Enter Checkout Details   │
│ - Customer name          │
│ - Payment method         │
│ - Amount paid            │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ POST /api/pos/checkout   │
│ Validate all items       │
└──────────┬───────────────┘
           │
    ┌──────┴───────┐
    │              │
  Invalid        Valid
    │              │
    ▼              ▼
Return Error  ┌──────────────────┐
Message       │ DB Transaction   │
              │ BEGIN            │
              └────────┬─────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │ 1. Create Sales      │
            │    Record            │
            │    - Invoice #       │
            │    - Total amount    │
            └────────┬─────────────┘
                     │
                     ▼
            ┌──────────────────────┐
            │ 2. Create Sale Items │
            │    (Loop through)    │
            │    - qty, price      │
            └────────┬─────────────┘
                     │
                     ▼
            ┌──────────────────────┐
            │ 3. Update Stock      │
            │    (Decrease)        │
            │    - Check available │
            │    - Decrement qty   │
            └────────┬─────────────┘
                     │
            ┌────────┴────────┐
            │                 │
          OK         Stock Shortage
            │                 │
            ▼                 ▼
      COMMIT          ROLLBACK
      Transaction     Transaction
            │                 │
            ▼                 ▼
      ┌──────────┐      ┌──────────┐
      │ Return   │      │ Return   │
      │ Success  │      │ Error    │
      │ + Invoice│      │ (HTTP 422)
      │ (HTTP 201)│      └──────────┘
      └────┬─────┘
           │
           ▼
      ┌──────────────────┐
      │ Print Receipt    │
      │ - Invoice #      │
      │ - Items          │
      │ - Total & Change │
      └──────────────────┘
```

## 🎨 UI Component Hierarchy

```
Layout: base.php
│
├─ NAVBAR
│  ├─ Brand Logo
│  └─ User Dropdown
│     ├─ Profile
│     ├─ Change Password
│     └─ Logout
│
├─ SIDEBAR
│  ├─ Dashboard
│  ├─ Operations
│  │  ├─ Point of Sale
│  │  └─ Sales
│  ├─ Inventory
│  │  ├─ Products
│  │  ├─ Suppliers
│  │  └─ Purchase Orders
│  ├─ Reporting
│  │  └─ Reports
│  └─ Administration
│     ├─ Users
│     └─ Settings
│
└─ MAIN CONTENT
   ├─ Alert Messages (success/error/warning)
   └─ Page Specific Content
      └─ Could be:
         ├─ Dashboard View
         ├─ Products Management
         ├─ Sales History
         ├─ POS Interface
         ├─ Reports
         └─ Admin Panels
```

## 📈 Session Lifecycle

```
User Not Logged In
    │
    ├─ Try to access /dashboard
    │  └─ AuthFilter detects no session
    │     └─ Redirect to /auth/login
    │
    ▼
┌──────────────────┐
│ User Logs In     │
│ Credentials OK   │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────┐
│ Session Created:             │
│ - user_id: 1                 │
│ - username: "admin"          │
│ - email: "admin@fishing.com" │
│ - role: "admin"              │
│ - logged_in: true            │
│ - expires: 2 hours           │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────┐
│ Access Granted   │
│ Can view all     │
│ protected pages  │
└────────┬─────────┘
         │
    ┌────┴────────────────────────┐
    │                             │
    ▼                             ▼
Continue browsing        Session expires
    │                     (2 hours or logout)
    │                             │
    │                             ▼
    │                    ┌──────────────────┐
    │                    │ Session cleared  │
    │                    │ - All data removed
    │                    │ - Cookie deleted │
    │                    └────────┬─────────┘
    │                             │
    └──────────┬──────────────────┘
               │
               ▼
┌──────────────────────┐
│ User Not Logged In   │
│ Redirect to login    │
└──────────────────────┘
```

## 🔐 Password Security

```
User Registration
       │
       ▼
Input: "password123"
       │
       ▼
┌─────────────────────────────────────────┐
│ password_hash($input, PASSWORD_BCRYPT)  │
│ (One-way encryption)                    │
└────────┬────────────────────────────────┘
         │
         ▼
Stored: "$2y$10$..." (60+ characters)
         │
         ▼
         Storage in Database

                    ↓ (Later)

User Login
       │
       ▼
Input: "password123"
       │
       ▼
┌─────────────────────────────────────────┐
│ password_verify($input, $stored_hash)   │
│ (Check if input matches hash)           │
└────────┬────────────────────────────────┘
         │
    ┌────┴────┐
    │          │
  Match     No Match
    │          │
    ▼          ▼
  Login OK  Login FAIL
   Allow    Deny
```

## 📱 Responsive Design

```
Desktop View (≥ 768px)
┌─────────────────────────────────────┐
│         NAVBAR (100%)                │
├──────────────┬──────────────────────┤
│  SIDEBAR     │                       │
│  (2 cols)    │  MAIN CONTENT (10)   │
│              │                       │
│              │                       │
│              │                       │
├──────────────┴──────────────────────┤
│         FOOTER                       │
└─────────────────────────────────────┘

Mobile View (< 768px)
┌──────────────┐
│    NAVBAR    │
├──────────────┤
│  MAIN        │
│  CONTENT     │
│  (Full       │
│   Width)     │
├──────────────┤
│   SIDEBAR    │
│   (Collapsed │
│    or Stack)│
└──────────────┘
```

## 🚀 Deployment Flow

```
Development
├─ Code changes
├─ Local testing
└─ Commit to repository

                ▼

Staging
├─ Run migrations
├─ Seed test data
├─ Run test suite
└─ QA testing

                ▼

Production
├─ Run migrations
├─ Seed initial data
├─ Enable security headers
├─ Monitor performance
└─ Backup regularly
```

## 📊 Performance Considerations

```
Request → AuthFilter ──┐
            (Fast)    │
                       ▼
         Controller → Model ──┐
         (Business Logic)     │
                              ▼
         Database Query ──┐
         (Indexed)        │
                          ▼
         View Rendering ──┐
         (HTML)           │
                          ▼
         Response ←───────┘
         (JSON/HTML)
```

---

All diagrams above represent the FISHING System architecture and flows.
